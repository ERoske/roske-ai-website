---
/**
 * WeeklyAnimation.astro
 *
 * Disabled by default. Edward's preference: drifting/falling particle
 * animations feel dated. Only re-enable for themes where ambient motion
 * makes obvious sense (e.g., a video-game week, a snow-globe holiday week).
 *
 * To re-enable for a specific week: replace the empty render below with the
 * themed animation script and set a self-contained <script is:inline>.
 */
---

<!-- intentionally empty -->
